<?php
if ( ! defined( 'ABSPATH' ) ) exit;
/** Service Area CPT — admin columns module (stub, safe no-op) */
// Custom admin columns for the Service Area CPT can be added here.
// Left as a no-op stub so the module loader does not log missing file errors.
